/**
 * @module utils
 */
/**
 * Returns a promise that is done after the given time
 */
export declare function delay(time: number): Promise<{}>;
/**
 * Returns a promise that is done after the given time
 */
export declare function waitFor(time: number): Promise<{}>;
